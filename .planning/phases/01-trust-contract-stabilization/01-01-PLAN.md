---
phase: 01-trust-contract-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api-java/src/main/java/org/opencivic/signalos/web/SignalController.java
  - apps/api-java/src/test/java/org/opencivic/signalos
  - docs/community/current-backlog.md
autonomous: true
requirements:
  - PRIO-01
  - TRST-01
user_setup: []
must_haves:
  truths:
    - Public prioritized and top-10 endpoints handle anonymous access without server exceptions.
    - Existing authenticated behavior remains unchanged.
  artifacts:
    - Controller null-safe viewer mapping path.
    - Regression tests covering anonymous access behavior.
  key_links:
    - Security permitAll rules and controller auth usage are behaviorally aligned.
---

<objective>
Harden trust-critical public signal endpoints against anonymous principal failures.

Purpose: prevent 500s in public civic backlog surfaces.
Output: null-safe controller mapping and regression tests.
</objective>

<execution_context>
@C:/Users/jdsal/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jdsal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api-java/src/main/java/org/opencivic/signalos/config/SecurityConfig.java
@apps/api-java/src/main/java/org/opencivic/signalos/web/SignalController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Null-safe response mapping for public signal reads</name>
  <files>apps/api-java/src/main/java/org/opencivic/signalos/web/SignalController.java</files>
  <action>Replace direct `authentication.getName()` usage in publicly permitted reads with safe username resolution that supports null authentication.</action>
  <verify>Run API tests for prioritized and top-10 flows.</verify>
  <done>No anonymous request path can trigger NPE from authentication access.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for anonymous access</name>
  <files>apps/api-java/src/test/java/org/opencivic/signalos</files>
  <action>Add/extend MockMvc tests validating 200 response for anonymous public endpoints and expected payload schema.</action>
  <verify>`mvn -q test` in `apps/api-java`.</verify>
  <done>Tests fail before fix and pass after fix for anonymous-path regression.</done>
</task>

<task type="auto">
  <name>Task 3: Capture trust-fix evidence in backlog docs</name>
  <files>docs/community/current-backlog.md</files>
  <action>Add a short execution note under critical fix section linking this remediation to trust-critical backlog item.</action>
  <verify>Manual review of markdown section.</verify>
  <done>Backlog reflects completed mitigation and validation commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn -q test` passes in `apps/api-java`
- [ ] Public endpoint smoke call returns 200 without auth token
- [ ] No contract-breaking response shape regression observed
</verification>

<success_criteria>

- Public signal read endpoints are anonymous-safe
- Regression tests prove trust-critical behavior
- Backlog/doc traceability updated
</success_criteria>

<output>
After completion, create `.planning/phases/01-trust-contract-stabilization/01-01-SUMMARY.md`
</output>
