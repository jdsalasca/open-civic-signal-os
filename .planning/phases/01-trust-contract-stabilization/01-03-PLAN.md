---
phase: 01-trust-contract-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .github/workflows/agent-gate.yml
  - scripts/agent-preflight.mjs
  - docs/DEFINITION_OF_DONE.md
autonomous: true
requirements:
  - TRST-04
  - QUAL-01
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - CI gate behavior matches documented no-skip backend verification policy.
    - Preflight and policy docs describe the same required checks.
  artifacts:
    - Updated workflow removing test-skip mismatch.
    - Updated preflight behavior and docs alignment.
  key_links:
    - Quality policy text and automation enforcement are consistent.
---

<objective>
Align CI and local quality gates with documented trust-critical policy.

Purpose: avoid false green pipelines and ensure reproducible quality signals.
Output: gate/workflow/doc consistency for backend and trust-critical checks.
</objective>

<execution_context>
@C:/Users/jdsal/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jdsal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.github/workflows/agent-gate.yml
@scripts/agent-preflight.mjs
@docs/DEFINITION_OF_DONE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove skip-tests contradiction in CI gate</name>
  <files>.github/workflows/agent-gate.yml</files>
  <action>Replace backend package step that skips tests with policy-compliant verification approach (test execution or explicit compile+test sequence).</action>
  <verify>Validate workflow syntax and dry-run lint if available.</verify>
  <done>Agent gate no longer undermines documented test policy.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize local preflight behavior with CI policy</name>
  <files>scripts/agent-preflight.mjs</files>
  <action>Ensure preflight messaging and behavior clearly separate hard failures vs local env blockers while preserving strict mode path for CI parity.</action>
  <verify>Run `npm run agent:preflight` in standard and strict modes where possible.</verify>
  <done>Preflight communicates enforceable policy without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Update definition-of-done wording for explicit gate parity</name>
  <files>docs/DEFINITION_OF_DONE.md</files>
  <action>Add concise statement requiring local/CI gate parity for trust-critical checks.</action>
  <verify>Manual documentation review.</verify>
  <done>Policy and automation references are aligned in docs and workflows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Workflow yaml is valid
- [ ] `npm run agent:preflight` behavior is documented and reproducible
- [ ] Docs reflect implemented gate behavior
</verification>

<success_criteria>

- No CI workflow contradicts no-skip-test policy
- Preflight and CI quality expectations are coherent
- Trust-critical gate rules are explicitly documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-trust-contract-stabilization/01-03-SUMMARY.md`
</output>
