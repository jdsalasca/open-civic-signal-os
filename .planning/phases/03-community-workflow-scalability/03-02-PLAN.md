---
phase: 03-community-workflow-scalability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web-react/src/store/useCommunityStore.ts
  - apps/web-react/src/components/Layout.tsx
  - apps/web-react/src/tests/global-state-integrity.spec.ts
  - apps/web-react/src/tests/role-switch-ux.spec.ts
autonomous: true
requirements:
  - COMM-01
  - COMM-04
user_setup: []
must_haves:
  truths:
    - Active community and role selections persist across route transitions when still valid.
    - Membership refresh does not reset valid global context unexpectedly.
  artifacts:
    - Hardened store state transitions and cache invalidation behavior.
    - Playwright coverage for state persistence scenarios.
  key_links:
    - Layout membership loading and community store selection rules stay consistent.
---

<objective>
Harden global community state persistence and role continuity across navigation flows.

Purpose: prevent context resets that reduce trust and usability.
Output: stable store behavior and end-to-end persistence tests.
</objective>

<execution_context>
@C:/Users/jdsal/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jdsal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web-react/src/store/useCommunityStore.ts
@apps/web-react/src/components/Layout.tsx
@apps/web-react/src/tests/global-state-integrity.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten community-store persistence rules</name>
  <files>apps/web-react/src/store/useCommunityStore.ts</files>
  <action>Ensure refresh logic preserves still-valid active community and thread list state; fallback only when invalid.</action>
  <verify>Unit-like behavior checks via existing store consumers and e2e flows.</verify>
  <done>Store no longer resets valid context on routine membership refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Align layout membership loading with state integrity policy</name>
  <files>apps/web-react/src/components/Layout.tsx</files>
  <action>Refine membership fetch + switch flow to avoid accidental context churn during route transitions.</action>
  <verify>Manual route-switch smoke + Playwright scenario validation.</verify>
  <done>Route changes preserve selected context and immediate feedback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Expand persistence-focused Playwright coverage</name>
  <files>apps/web-react/src/tests/global-state-integrity.spec.ts, apps/web-react/src/tests/role-switch-ux.spec.ts</files>
  <action>Add assertions for community/role persistence across navigation, refresh, and filter interactions.</action>
  <verify>Run Playwright tests for the two targeted specs.</verify>
  <done>State integrity guarantees are test-backed for critical route flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Role/community persistence specs pass
- [ ] No regressions in membership loading UX
- [ ] Active context remains stable across route transitions
</verification>

<success_criteria>

- Global context integrity is preserved across main user journeys
- Layout/store interactions do not trigger unintended resets
- Persistence behavior is regression-protected with e2e evidence
</success_criteria>

<output>
After completion, create `.planning/phases/03-community-workflow-scalability/03-02-SUMMARY.md`
</output>
