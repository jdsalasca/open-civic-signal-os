# 05-13 PLAN - Settings Density and Copy Resilience

## Objective

Finish the Settings-side remediation of the cross-application stat-card problem by reducing card density, shortening fragile community labels, and ensuring longer Spanish strings stay legible inside shared layout primitives.

## Scope

- Split the overloaded Settings identity surface into clearer cards for identity, community belonging, and achievements.
- Reuse `CivicStatCard` in Settings membership summaries.
- Tighten global stat-card and meta-label wrapping rules for ES-heavy copy.
- Shorten community-facing labels that were still too long for compact summary surfaces.

## Acceptance Criteria

- Community summary labels no longer split awkwardly in Settings.
- Settings cards have clearer visual hierarchy and less vertical overload.
- Shared stat-card tokens continue to render safely in Dashboard, CommunityFeed, Communities, and SignalDetail.
- Frontend build passes in Docker and GSD state/changelog stay aligned.

## Verification

- `docker exec infra-civic-web-dev-1 sh -lc "cd /workspace && npm --workspace apps/web-react run build"`
- `npm run agent:context:check`
- `npm run backlog:current:check`
