---
phase: 02-quality-gate-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/release-quality-gate.mjs
  - coverage/coverage-summary.json
autonomous: true
requirements:
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Release gate enforces a non-zero minimum coverage threshold.
    - Coverage summary is meaningful for evaluated metrics.
  artifacts:
    - Updated release quality configuration and threshold.
    - Script behavior aligned with threshold semantics.
  key_links:
    - Quality release gate cannot pass with empty/placeholder coverage data.
---

<objective>
Strengthen coverage enforcement to make release-quality gate meaningful.

Purpose: avoid false confidence from zero-threshold coverage checks.
Output: non-zero threshold policy and aligned gate behavior.
</objective>

<execution_context>
@C:/Users/jdsal/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jdsal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
@scripts/release-quality-gate.mjs
@coverage/coverage-summary.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise minimum coverage threshold</name>
  <files>package.json</files>
  <action>Set `releaseQuality.minimumCoverage` to a non-zero baseline agreed for current maturity (e.g., 20+ initial floor).</action>
  <verify>Run `npm run quality:release` with current summary.</verify>
  <done>Release gate fails when coverage is below configured threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Improve gate diagnostics for empty/invalid coverage</name>
  <files>scripts/release-quality-gate.mjs</files>
  <action>Add explicit failure messaging for zero-total coverage and missing metric integrity to prevent ambiguous passes.</action>
  <verify>Run gate with existing summary and inspect error output quality.</verify>
  <done>Gate output clearly describes failing coverage dimensions and remediation.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh baseline coverage summary handling</name>
  <files>coverage/coverage-summary.json</files>
  <action>If this file is intentionally committed, ensure it reflects a valid generation process; otherwise document why static summary should not be tracked.</action>
  <verify>Team policy consistency check in docs/scripts.</verify>
  <done>Coverage artifact strategy is explicit and enforceable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run quality:release` fails on insufficient coverage and passes on valid threshold
- [ ] Gate output is actionable
- [ ] Coverage artifact policy is explicit
</verification>

<success_criteria>

- Coverage gate is non-trivial and enforceable
- Empty coverage summaries cannot silently satisfy quality release
- Teams understand how coverage evidence is produced and validated
</success_criteria>

<output>
After completion, create `.planning/phases/02-quality-gate-hardening/02-02-SUMMARY.md`
</output>
