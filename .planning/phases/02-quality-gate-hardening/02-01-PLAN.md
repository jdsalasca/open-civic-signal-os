---
phase: 02-quality-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/playwright-cli.mjs
  - docs/agent/playwright-visual-protocol.md
  - docs/community/current-backlog.md
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Playwright wrapper runs successfully on Windows and Linux paths or performs reliable fallback.
    - Contributor workflow for `agent:ux:pw` is deterministic and documented.
  artifacts:
    - Hardened wrapper script with cross-platform path/process handling.
    - Updated protocol docs with fallback examples.
  key_links:
    - Mandatory UX evidence gate can be satisfied on supported environments.
---

<objective>
Harden Playwright wrapper reliability for cross-platform contributor environments.

Purpose: unblock mandatory UX evidence collection.
Output: robust wrapper fallback and updated usage protocol.
</objective>

<execution_context>
@C:/Users/jdsal/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/jdsal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@scripts/playwright-cli.mjs
@docs/agent/playwright-visual-protocol.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix wrapper shell invocation and fallback flow</name>
  <files>scripts/playwright-cli.mjs</files>
  <action>Adjust command execution strategy to avoid Windows bash dependency failures and ensure clean fallback to `npx @playwright/cli` when wrapper is unavailable/broken.</action>
  <verify>Run `npm run agent:ux:pw -- --help` and `open` smoke call in local environment.</verify>
  <done>Wrapper no longer fails with path-related EPERM/E_ACCESSDENIED in standard setups.</done>
</task>

<task type="auto">
  <name>Task 2: Update protocol documentation for fallback semantics</name>
  <files>docs/agent/playwright-visual-protocol.md</files>
  <action>Document explicit Windows fallback behavior and troubleshooting sequence tied to wrapper implementation.</action>
  <verify>Manual docs review with command copy/paste sanity.</verify>
  <done>Protocol provides deterministic recovery path for wrapper failures.</done>
</task>

<task type="auto">
  <name>Task 3: Mark backlog item with implementation status</name>
  <files>docs/community/current-backlog.md</files>
  <action>Add execution status note under Windows wrapper critical fix entry, including validation command outcomes.</action>
  <verify>Backlog markdown check.</verify>
  <done>Operational backlog reflects implementation progress and evidence reference.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run agent:ux:pw -- --help` succeeds
- [ ] `npm run agent:ux:pw -- open <url>` path tested (or documented blocker)
- [ ] Updated protocol reflects real behavior
</verification>

<success_criteria>

- UX evidence tooling is operational across contributor environments
- Documentation mirrors actual wrapper behavior
- Backlog audit trail updated
</success_criteria>

<output>
After completion, create `.planning/phases/02-quality-gate-hardening/02-01-SUMMARY.md`
</output>
